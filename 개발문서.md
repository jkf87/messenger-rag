# 충북소통메신저 백업 및 검색 도구 — 개발 문서

> 작성일: 2026-03-19 / 최종수정: 2026-03-20
> 개발 환경: Windows 11, Python 3.14, 충북소통메신저(CBE Talk)

---

## 1. 개요

충북소통메신저(CBE Talk)는 CEF(Chromium Embedded Framework) 기반 데스크탑 메신저다.
내부 웹페이지를 Chromium 엔진이 렌더링하는 구조이므로, **Chrome DevTools Protocol(CDP)** 을 통해
JavaScript를 원격 실행하여 메시지·쪽지 데이터를 추출할 수 있다.

이 도구는 다음 기능을 제공한다.

| 기능 | 설명 |
|------|------|
| **백업** | 채팅 메시지 / 쪽지 전체를 SQLite DB에 저장 |
| **첨부파일 수집** | 쪽지 첨부파일 전체를 CDP 배치로 자동 다운로드 |
| **HWP 변환** | HWP → HWPX 일괄 변환 (한글 COM 자동화) |
| **검색** | 키워드 검색 + 로컬 RAG(시맨틱) 검색 GUI 앱 |

---

## 2. 시스템 구조

```
충북소통메신저 (CEF 앱)
    │
    │  CDP (WebSocket, port 9000)
    ▼
Python 스크립트
    │  JS 실행 → DOM / AJAX 응답 수신
    │
    ├─► SQLite DB (messages.db)
    │       messages 테이블 (채팅)
    │       notes    테이블 (쪽지)
    │       note_attachments 테이블 (첨부파일 메타)
    │
    ├─► attachments/ (첨부파일 원본, note_code별 폴더)
    │
    └─► embed_index.pkl (임베딩 벡터 인덱스)
             │
             ▼
    search_app_win.py (tkinter GUI 검색앱)
        키워드 검색 / 로컬 RAG 검색 전환
```

### CDP 연결 방식

메신저를 CDP 원격 디버깅 모드로 실행해야 한다.
메신저 실행 파일 바로가기에 다음 인수를 추가한다.

```
--remote-debugging-port=9000
```

연결 확인: 브라우저에서 `http://127.0.0.1:9000/json` 접속 시 페이지 목록 JSON이 반환되면 정상.

---

## 3. 파일 구성

```
messenger_rag/
├── backup.py              # 실시간 모니터링 백업 (채팅)
├── backup_all.py          # 전체 일괄 백업 (채팅방·1:1)
├── backup_notes.py        # 쪽지 전체 백업
├── batch_download.py      # 쪽지 첨부파일 배치 다운로드 (221건)
├── convert_hwp_to_hwpx.py # HWP → HWPX 일괄 변환 (pyhwpx)
├── build_index.py         # 로컬 RAG 임베딩 인덱스 빌드
├── local_rag.py           # 로컬 RAG 검색 모듈
├── compare_search.py      # 키워드 vs RAG 성능 비교
├── search_comparison.md   # 비교 결과 문서
├── search_app_win.py      # Windows 검색 GUI 앱
├── 1_백업_실행.bat        # backup.py 실행 바로가기
├── 2_검색앱_실행.bat      # search_app_win.py 실행 바로가기
├── 3_쪽지백업_실행.bat    # backup_notes.py 실행 바로가기
├── requirements.txt       # 의존 패키지
├── .gitignore
├── messages.db            # SQLite DB (gitignore 제외)
├── embed_index.pkl        # 임베딩 인덱스 (gitignore 제외)
└── attachments/           # 첨부파일 저장 폴더 (gitignore 제외)
```

---

## 4. DB 스키마

### messages 테이블 (채팅 메시지)

```sql
CREATE TABLE messages (
    id        INTEGER PRIMARY KEY AUTOINCREMENT,
    room_code TEXT,          -- 채팅방 고유 코드
    room_name TEXT,          -- 채팅방 이름
    sender    TEXT,          -- 발신자 닉네임
    content   TEXT,          -- 메시지 내용
    msg_time  TEXT,          -- 시간 (HH:MM)
    msg_date  TEXT,          -- 날짜 (YYYY년 M월 D일)
    saved_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(room_code, sender, content, msg_time, msg_date)
);
CREATE INDEX idx_content ON messages(content);
```

### notes 테이블 (쪽지)

```sql
CREATE TABLE notes (
    id        INTEGER PRIMARY KEY AUTOINCREMENT,
    note_code TEXT UNIQUE,   -- 쪽지 고유 코드 (NTE_CODE)
    note_type TEXT,          -- 'receive' | 'send'
    sender    TEXT,          -- 발신자 (NUR_NIC)
    receiver  TEXT,          -- 수신자 (RNIC)
    title     TEXT,          -- 제목 (NTE_TITLE)
    content   TEXT,          -- 내용 (NTE_CONTENT, HTML 태그 제거됨)
    note_date TEXT,          -- 날짜 (NTE_REGDATE: YYYYMMDDHHMMSSMMM)
    read_yn   TEXT,          -- 읽음 여부
    file_cnt  INTEGER DEFAULT 0  -- 첨부파일 수
);
CREATE INDEX idx_note_content ON notes(content);
CREATE INDEX idx_note_title   ON notes(title);
```

> **note_code 중복 처리**: 받은쪽지와 보낸쪽지는 동일한 NTE_CODE를 공유한다.
> `UNIQUE(note_code)` 제약으로 중복 저장이 자동 방지된다.

---

## 5. 백업 스크립트 상세

### 5-1. backup.py — 실시간 모니터링 백업

**동작 방식**
1. `http://127.0.0.1:9000/json` 에서 열린 페이지 목록 조회 (3초 간격)
2. URL에 `messageDetail`이 포함된 페이지(채팅창) 감지
3. 새 창 감지 시 즉시, 기존 창은 30초 간격으로 백업
4. CDP WebSocket으로 `document.body.innerText` 읽어 파싱

**메시지 파싱 로직** (`parse_messages`)

채팅창 innerText는 다음과 같은 패턴으로 구성된다.

```
2026년 3월 19일
홍길동
안녕하세요
10:30
이 부분을 처리해야 합니다
10:31
나
알겠습니다
10:32
```

파싱 규칙:
- `YYYY년 M월 D일` 패턴 → 날짜 구분선
- `HH:MM` 패턴 → 직전 메시지의 시간
- 시간 바로 앞 줄이 짧고 이전이 시간/날짜인 경우 → 발신자 이름
- 그 외 → 메시지 내용

**중복 방지**: `INSERT OR IGNORE`와 `UNIQUE(room_code, sender, content, msg_time, msg_date)` 조합으로 동일 메시지 재저장 방지.

---

### 5-2. backup_all.py — 전체 일괄 백업

**동작 방식**
1. **0단계**: 현재 열린 `messageDetail` 창 즉시 수집
2. **1단계**: 그룹 채팅방
   - `POST /ezmaru/pc/chatroom/chatroomlist` API로 채팅방 목록 조회
   - DOM에서 채팅방 클릭 또는 `chatroomdetail_controller.chatRoomInfo(code)` 직접 호출
   - 새 `messageDetail` 창이 열리길 대기 후 innerText 수집
3. **2단계**: 1:1 메시지
   - `POST /ezmaru/pc/message/sessionlist` API 호출 (AES-256 암호화 전송/수신)
   - `cryptoUtil.encryptAES256WithSHA256Key` / `decryptAES256WithSHA256Key` 사용
   - 각 세션 클릭 또는 `msgbox_controller.detailItemView(code)` 호출

---

### 5-3. backup_notes.py — 쪽지 전체 백업

**동작 방식**

```python
PAGE_SIZE = 1000

# 쪽지 탭 활성화
ev_sync(ws, 1, 'document.querySelector("a.note").click();')

# 쪽지 목록 API 호출 (페이지네이션)
$.ajax({
    url: '/ezmaru/pc/note/noteList',
    type: 'POST',
    async: false,
    data: { pageSize: 1000, pageNo: N, noteType: 'receive' | 'send' }
})
```

**API 응답 필드 매핑**

| API 필드 | DB 컬럼 | 설명 |
|----------|---------|------|
| `NTE_CODE` | `note_code` | 쪽지 고유 식별자 |
| `NTE_TITLE` | `title` | 제목 |
| `NTE_CONTENT` / `CONTENTS` | `content` | 본문 (HTML 태그 제거) |
| `NTE_REGDATE` | `note_date` | 등록일시 (17자리: YYYYMMDDHHMMSSMMM) |
| `NUR_NIC` / `NUR_USERID` | `sender` | 발신자 |
| `RNIC` | `receiver` | 수신자 |
| `READ_YN` | `read_yn` | 읽음 여부 |
| `FILE_CNT` | `file_cnt` | 첨부파일 수 |

**HTML 태그 제거**

쪽지 본문에 HTML이 포함되어 있어 정규식으로 제거한다.

```python
content = re.sub(r'<[^>]+>', '', content)
content = content.replace("&nbsp;", " ").replace("&lt;", "<").replace("&gt;", ">").replace("&amp;", "&")
```

**페이지네이션 종료 조건**: 반환 건수 < PAGE_SIZE(1000) 이면 마지막 페이지로 판단하고 중단.

---

## 6. CDP 공통 유틸리티

### get_pages()
```python
urllib.request.urlopen("http://127.0.0.1:9000/json")
# → 열린 페이지 목록 반환 (id, url, webSocketDebuggerUrl 포함)
```

### ev_sync(ws, id_, expr)
WebSocket으로 JS를 동기 실행하고 반환값을 문자열로 받는다.
```python
ws.send(json.dumps({
    "id": id_,
    "method": "Runtime.evaluate",
    "params": {"expression": expr}
}))
result = json.loads(ws.recv())["result"]["result"]["value"]
```

---

## 7. 검색 앱 (search_app_win.py)

### UI 구조

```
┌─────────────────────────────────────────────┐
│ 헤더: 제목 + 통계 (메시지N건 / 쪽지N건)       │
├─────────────────────────────────────────────┤
│ [💬 채팅 메시지]  [📨 쪽지]  ← 모드 탭       │
├─────────────────────────────────────────────┤
│ [검색어 입력창________________________] [검색] │
│ 채팅방: [드롭다운▼]  발신자: [___]  (메시지탭) │
│ 종류: [전체▼]  발신자: [___]          (쪽지탭) │
├─────────────────────────────────────────────┤
│ 결과 N건 (키워드/AI 의미검색)                  │
├──────────────── ↕ 드래그 ───────────────────┤
│ TreeView (검색 결과 목록)                     │
│   [발신자 | 채팅방 | 내용 | 시간 | 날짜]       │
│   [종류 | 보낸이 | 받는이 | 제목 | 날짜 | 첨부] │
├──────────────── ↕ 드래그 ───────────────────┤
│ 선택 내용:                       [복사]       │
│ ┌─────────────────────────────────────────┐ │
│ │ 선택한 항목의 전체 내용 표시 (스크롤 가능) │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 모드 전환 (_switch_mode)

`💬 채팅 메시지` / `📨 쪽지` 탭 버튼으로 전환.
- 해당 탭의 필터 행(row_filter)과 TreeView를 `pack` / `pack_forget`으로 교체
- `PanedWindow` 내 `tree_outer` 컨테이너 안에서 트리 프레임 교체

### 크기 조절 패널 (PanedWindow)

```python
self._paned = tk.PanedWindow(self, orient="vertical", sashwidth=6)
self._paned.add(tree_outer,    stretch="always", minsize=120)  # 트리 영역
self._paned.add(detail_frame,  stretch="never",  minsize=80)   # 상세 영역
```
사시(sash)를 마우스로 드래그해 트리/상세 패널 높이 비율 조절 가능.
기본 상세 패널 높이: 150px (앱 시작 시 `sash_place` 로 설정).

### 복사 기능 (_copy_detail)

```python
def _copy_detail(self):
    text = self.txt_detail.get("1.0", "end-1c").strip()
    self.clipboard_clear()
    self.clipboard_append(text)
    self._lbl_copied.config(text="복사됨 ✓")
    self.after(2000, lambda: self._lbl_copied.config(text=""))
```
복사 버튼 클릭 시 상세 내용 전체를 클립보드에 복사하고 2초간 확인 메시지 표시.

### 검색 로직

**키워드 검색 (기본)**

```python
SELECT ... FROM notes
WHERE (title LIKE '%키워드%' OR content LIKE '%키워드%')
  AND note_type = 'receive'   -- 종류 필터
  AND sender LIKE '%이름%'    -- 발신자 필터
ORDER BY note_date DESC LIMIT 200
```

**AI 의미검색 (선택, ANTHROPIC_API_KEY 설정 시)**

환경변수 `ANTHROPIC_API_KEY`가 설정되어 있으면 Claude API(claude-haiku-4-5)를 활용한다.
1. 키워드 검색으로 후보 최대 80건 추출
2. Claude에게 관련성 순 인덱스 번호 반환 요청
3. 반환된 순서로 결과 재정렬

### 날짜 포맷 변환

쪽지의 `note_date` 필드는 17자리 문자열이다.

```python
def fmt_note_date(d):
    # "20260319151418891" → "2026-03-19 15:14"
    return f"{d[0:4]}-{d[4:6]}-{d[6:8]} {d[8:10]}:{d[10:12]}"
```

### 쪽지 전체 내용 조회

TreeView에는 제목만 표시하고, 항목 선택 시 DB에서 전체 내용을 별도 조회해 하단 패널에 표시한다.
TreeView 아이템의 `tags`에 `note_code`를 저장해 두었다가 조회 키로 사용.

```python
self.tree_note.insert("", "end",
    values=(type_label, sender, receiver, title, date_str, file_str),
    tags=(note_code,))  # ← note_code를 tag에 숨김

# 선택 이벤트
note_code = self.tree_note.item(sel[0], "tags")[0]
row = conn.execute("SELECT title, content FROM notes WHERE note_code=?", (note_code,)).fetchone()
```

---

## 8. 실행 방법

### 사전 준비

1. 메신저 실행 바로가기에 CDP 포트 인수 추가
   ```
   --remote-debugging-port=9000
   ```

2. Python 패키지 설치
   ```bash
   pip install websocket-client
   # tkinter는 Python 기본 포함
   # anthropic은 AI 검색 사용 시에만 필요
   ```

### 실행 순서

| 순서 | 파일 | 설명 |
|------|------|------|
| 1 | `3_쪽지백업_실행.bat` | 쪽지 전체 수집 (1회 실행) |
| 2 | `1_백업_실행.bat` | 채팅 실시간 백업 (백그라운드 상시 실행) |
| 3 | `2_검색앱_실행.bat` | 검색 GUI 앱 실행 |

> **주의**: 메신저가 실행·로그인된 상태에서만 백업 가능.

### AI 검색 활성화 (선택)

```bash
set ANTHROPIC_API_KEY=sk-ant-...
python search_app_win.py
```

---

## 9. 주요 기술 결정 사항

### CDP 선택 이유
CBE Talk는 폐쇄망 앱으로 공개 API가 없다. CEF 기반이므로 Chromium 디버깅 프로토콜로
앱 내부 JS 실행 권한을 획득하는 것이 유일한 현실적 방법이다.

### innerText 파싱 vs. API 호출
- **채팅 메시지**: API가 암호화(AES-256)되어 있어 직접 호출이 어렵고,
  채팅창 innerText 파싱이 더 안정적이다.
- **쪽지**: `noteList` API가 평문 JSON을 반환하므로 API 직접 호출로 처리.

### INSERT OR IGNORE 전략
같은 메시지가 여러 번 수집될 수 있으므로 UNIQUE 제약 + `INSERT OR IGNORE`로
멱등성(idempotent)을 보장한다. 재실행해도 DB가 오염되지 않는다.

### note_code 중복 (받은/보낸쪽지)
서버는 받은쪽지·보낸쪽지에 동일한 `NTE_CODE`를 부여한다.
`UNIQUE(note_code)` 제약으로 자동 중복 처리되며,
내용이 동일하므로 데이터 손실이 없다.

---

## 10. 첨부파일 배치 다운로드 (batch_download.py)

### 동작 방식

CDP WebSocket을 통해 noteDetail 창의 JS 함수를 직접 호출해 쪽지를 순회하며 첨부파일을 자동 다운로드한다.

```
DB에서 file_cnt > 0 쪽지 목록 조회
    └─► 각 쪽지별:
        1. $('input[name=code]').val(note_code)  ← hidden input 변경
        2. noteDetail_controller.noteDetail()    ← JS 함수 호출 (EZAjax 내부 처리)
        3. DOM 폴링: [fileurl] 속성 요소 감지 대기 (최대 6초)
        4. {fileurl}/{oriname} + 쿠키 → Python urllib 다운로드
        5. attachments/{note_code}/{파일명} 저장
        6. note_attachments 테이블 INSERT OR IGNORE
```

### 핵심 발견 사항

- `noteDetail_controller.noteDetail()` 함수가 내부적으로 `EZAjax({nteCode: ...})`를 처리
- 파일 URL: `/file/v2/{session_token}/upload/note/{file_id}.{ext}`
- `oriname` 속성: Base64 인코딩된 원본 파일명 → `base64.b64decode(oriname).decode("utf-8")`
- 세션 쿠키(`jsessionid`)만 있으면 Python urllib로 직접 다운로드 가능

### note_attachments 테이블

```sql
CREATE TABLE note_attachments (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    note_code    TEXT,
    file_name    TEXT,
    file_size    INTEGER,
    local_path   TEXT,
    fileurl      TEXT,
    oriname      TEXT,
    download_ok  INTEGER DEFAULT 1,
    downloaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(note_code, oriname)
);
```

### 실행 결과

- 대상: 221건 (file_cnt > 0 쪽지)
- 결과: **286개 파일 다운로드 성공**, 실패 2건
- 저장 위치: `attachments/{note_code}/원본파일명`
- 10개마다 WS 재연결로 안정성 확보

---

## 11. HWP → HWPX 변환 (convert_hwp_to_hwpx.py)

### 배경

쪽지 첨부파일 대부분이 HWP 바이너리 형식이며, HWPX(XML 기반)로 변환하면 구조 분석·텍스트 추출이 용이하다.

### 구현 방식

`pyhwpx` 라이브러리를 통해 Hancom HWP COM 자동화로 변환한다.
한글 프로그램이 설치된 Windows 환경에서만 동작한다.

```python
import pyhwpx as hwp

h = hwp.Hwp()
h.open(str(hwp_path))
h.save_as(str(hwpx_path), format="HWPML2X")  # HWPML2X = HWPX 포맷
h.quit()
```

### 실행 결과

- 대상: `attachments/` 하위 `.hwp` 파일 122개
- 결과: **121개 변환 성공**, 1개 건너뜀(이미 변환), 실패 0개
- 출력: 원본과 동일 경로에 `.hwpx` 파일 생성

---

## 12. 로컬 RAG 검색

### 개요

인터넷·외부 API 없이 CPU만으로 동작하는 시맨틱 검색이다.
`intfloat/multilingual-e5-small` 모델(117MB)로 쪽지·메시지를 임베딩하고
코사인 유사도로 의미적으로 유사한 문서를 검색한다.

### 구성 파일

| 파일 | 역할 |
|------|------|
| `build_index.py` | 모델 다운로드 + 전체 임베딩 생성 + `embed_index.pkl` 저장 |
| `local_rag.py` | 검색 모듈 (인덱스 로드 + 코사인 유사도 계산) |
| `embed_index.pkl` | 임베딩 벡터 + 원본 데이터 (2.1MB) |

### 임베딩 규칙 (multilingual-e5 권장)

```python
# 인덱스 빌드 시 (문서)
f"passage: {title} {content[:300]}"

# 검색 시 (쿼리)
f"query: {검색어}"
```

### 검색 로직

```python
import numpy as np

qv = model.encode([f"query: {q}"], normalize_embeddings=True)[0]
scores = note_embs @ qv          # 정규화 벡터 내적 = 코사인 유사도
order  = np.argsort(scores)[::-1]  # 유사도 내림차순
```

### 성능 (CPU, Windows 11)

| 단계 | 소요 시간 |
|------|---------|
| 최초 모델 로드 | ~15초 (이후 캐시) |
| 인덱스 빌드 (996건) | 21.6초 |
| 검색 쿼리 1건 | ~10ms |

### 키워드 vs RAG 비교 결과

7개 테스트 쿼리 기준 (`search_comparison.md` 참조):

| 방식 | 히트 0건 쿼리 | 특징 |
|------|-------------|------|
| 키워드 (LIKE) | 5/7개 | 띄어쓰기·유의어 변형에 취약 |
| 로컬 RAG | 0/7개 | 의미 기반, 모든 쿼리에서 관련 문서 발견 |

예시: "개인정보 보호" → 키워드 0건, RAG는 "개인정보보호서약서" 등 정확히 탐색.

### 앱 통합

검색창 옆 **"로컬 RAG" 체크박스**로 전환:
- 체크 ON: 시맨틱 검색 (`local_rag.py`)
- 체크 OFF: 키워드 검색 (SQLite LIKE)
- `embed_index.pkl` 없으면 체크박스 비활성화

### 인덱스 재빌드

새 쪽지가 쌓인 경우 재실행:
```bash
python build_index.py
```
학교 네트워크(SSL 프록시) 환경에서는 스크립트 상단의 httpx SSL 패치가 자동 적용됨.

---

## 13. 알려진 제한 사항

| 항목 | 내용 |
|------|------|
| 과거 메시지 스크롤 | 각 채팅창에서 스크롤 업으로 과거 메시지를 로드해야 함. 자동 스크롤 미구현. |
| 1:1 메시지 일괄 수집 | `sessionlist` API의 `LIST_COUNT: -1` 응답으로 파싱 실패 케이스 존재. |
| 그룹채팅방 창 열기 | DOM 클릭으로 `messageDetail` 창이 열리지 않는 경우가 있음. |
| 첨부파일 | batch_download.py로 전체 수집 완료. 단, 세션 만료 시 재실행 필요. |
| RAG 인덱스 | 새 쪽지 수집 후 build_index.py 재실행 필요 (자동 갱신 미구현). |
| HWP 변환 | 한글 프로그램 설치된 Windows에서만 동작. |
| CDP 포트 충돌 | 메신저 재시작 시 CDP 연결이 끊기므로 백업 스크립트도 재시작 필요. |
